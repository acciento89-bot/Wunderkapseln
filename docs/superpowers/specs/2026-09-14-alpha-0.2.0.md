# Wunderkapseln alpha 0.2.0 - approved continuation

Preserve the approved miniature-world aesthetic, native iOS/Android clients and the shared offline game core. Work only in this isolated local feature branch: the previous GitHub upload was platform-blocked, and this iteration must not try another write route.

## Deliverables
1. Lifecycle correctness: accept moves only in loaded, foreground, unpaused gameplay; safe result/back navigation; deduplicated hydration; system reduced-motion is transient, not a saved user choice. Replace broken native star glyphs and test source text integrity.
2. Twelve distinct architectural landmarks, not palette variants. Preserve the glass dome, gem art and tactile composition. Each world has five restoration milestones at 8/16/24/32/40 unique level wins. No extra paid currency or reward for replaying milestones. Localized milestone names and visible progress in native and preview screens.
3. Original synthesized match/chain/wonder/win/loss sounds, mute setting, playback only in the foreground, and stopped audio in menus or background. Shared event policy plus a browser adapter and Expo Audio native adapter. Never request microphone or background playback permission. No audio/analytics network traffic.
4. Re-run core tests, full opening-board validation, browser interactions, art XML checks, audio format checks and source syntax checks. Refresh portable preview and source archive. Keep actual APK/AAB/IPA, store payments, advertising, human balancing, and native device QA explicitly open until observed.

## Constraints
SDK 55 stays in place. No dependency upgrades unrelated to this scope. Five lives, 25-minute regeneration, 480 campaign recipes, explicit active-time activation and canonical launcher/splash assets stay unchanged. Existing version-1 saves load with sound enabled by default; an explicit saved mute persists. Effects cannot block or mutate gameplay. No real-money entitlements are granted.
